import Alamofire

class HTTPControler {
/*
    public var Contacts: [String]

    public func getUserContacts(userId: Int) -> Array<String>{
        var contacts : [String]
        //Exemple d'URL ou l'on ira récupérer nos données
        let url = URL(string: "http://www.ourApp.com/\(userId)/contacts")!

        //On effectue une GET Request à l'url correspondante pour récupérer les contacts
        Alamofire.request(url).responseJSON { (responseData) -> Void in
        if((responseData.result.value) != nil) {

            //On récupère la réponse du server au sein d'un fichier JSON
            let swiftyJsonVar = JSON(responseData.result.value!)
            
            //Sérialisation du JSON en un array
            if contacts = swiftyJsonVar["contacts"].arrayObject {

                //On va mettre à jour les contacts de l'utilisateur si ce qui a été récupéré est différent
                //if contacts.count > 0 && contacts != self.Contacts{
                    self.Contacts = contacts
            }
            }
        }
    }*/
}
